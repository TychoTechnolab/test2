name: Main Workflow

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  backend:
    uses: ./.github/workflows/backend.yml
    permissions:
      packages: write
      id-token: write

  flutter:
    uses: ./.github/workflows/flutter.yml
    permissions:
      contents: write
